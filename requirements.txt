fastapi
uvicorn[standard]
jinja2
requests
itsdangerous
python-multipart

# Postgres (con connection pool) + hashing de passwords + Resend
psycopg[binary,pool]
bcrypt
resend

# Gateways de pago (scaffolded — solo se activan si pones las API keys)
stripe
mercadopago
